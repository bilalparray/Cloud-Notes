rules_version = '2';
// Allow authenticated users to read/write note images under workspaces.
// Deploy with: firebase deploy --only storage
service firebase.storage {
  match /b/{bucket}/o {
    match /workspaces/{workspaceId}/notes/{noteId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
